spring.application.name=currency-conversion
server.port=8100

#For versions>2.4.2, we need to add this line, as we have config 
#client dependency added, even if config server is not up& running, 
#there wont be any issue as we added optional
spring.config.import=optional:configserver:http://localhost:8888


eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka